/*
  JS-side reduced-motion gate matching the CSS guards in globals.css.
  Durations, easings and staggers live in CSS custom properties.
*/
export const MM_MOTION = "(prefers-reduced-motion: no-preference)";
